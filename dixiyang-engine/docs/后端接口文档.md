# Dixiyang Engine 后端接口文档

## 接口概述

本文档详细描述了 Dixiyang Engine 后端提供的所有 API 接口，包括请求方法、URL、参数说明和响应格式。

## 统一响应格式

所有 API 接口使用统一的响应格式：

```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

其中：
- `code`: 状态码，200 表示成功，其他值表示失败
- `msg`: 响应消息
- `data`: 响应数据

## 认证相关接口

### 1. 用户登录

**接口地址**: `POST /auth/login`

**请求参数**:
```json
{
  "username": "string",
  "password": "string"
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "testuser",
      "nickname": "测试用户",
      "email": "test@example.com"
    }
  }
}
```

### 2. 用户注册

**接口地址**: `POST /auth/register`

**请求参数**:
```json
{
  "username": "string",
  "password": "string",
  "nickname": "string",
  "email": "string"
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "注册成功！！！",
  "data": null
}
```

## 小说管理接口

### 1. 获取小说列表

**接口地址**: `GET /novel/listall?page=1&pageSize=10`

**请求参数**:
- `page`: 页码，默认为 1
- `pageSize`: 每页大小，默认为 10

**响应示例**:
```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "小说标题",
        "penName": "作者笔名",
        "description": "小说描述",
        "coverUrl": "封面URL",
        "userId": 1,
        "createTime": "2026-03-23T10:00:00",
        "updateTime": "2026-03-23T10:00:00",
        "charCount": 5,
        "nodeCount": 10,
        "relationCount": 2
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

### 2. 创建小说

**接口地址**: `POST /novel/create`

**请求参数**:
```json
{
  "title": "string",
  "penName": "string",
  "description": "string",
  "coverUrl": "string"
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "创建成功",
  "data": {
    "id": 1,
    "title": "小说标题",
    "penName": "作者笔名",
    "description": "小说描述",
    "coverUrl": "封面URL",
    "userId": 1,
    "createTime": "2026-03-23T10:00:00",
    "updateTime": "2026-03-23T10:00:00",
    "charCount": 0,
    "nodeCount": 0,
    "relationCount": 0
  }
}
```

### 3. 更新小说

**接口地址**: `POST /novel/update/{novelId}`

**路径参数**:
- `novelId`: 小说ID

**请求参数**:
```json
{
  "title": "string",
  "penName": "string",
  "description": "string",
  "coverUrl": "string"
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "更新成功",
  "data": {
    "id": 1,
    "title": "更新后的标题",
    "penName": "作者笔名",
    "description": "更新后的描述",
    "coverUrl": "封面URL",
    "userId": 1,
    "createTime": "2026-03-23T10:00:00",
    "updateTime": "2026-03-23T11:00:00",
    "charCount": 5,
    "nodeCount": 10,
    "relationCount": 2
  }
}
```

### 4. 删除小说

**接口地址**: `POST /novel/delete/{novelId}`

**路径参数**:
- `novelId`: 小说ID

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null
}
```

## 角色管理接口

### 1. 获取角色列表（分页）

**接口地址**: `GET /novelCharacter/list/{novelId}?page=1&pageSize=10`

**路径参数**:
- `novelId`: 小说ID

**查询参数**:
- `page`: 页码，默认为 1
- `pageSize`: 每页大小，默认为 10

**响应示例**:
```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "records": [
      {
        "id": 1,
        "novelId": 1,
        "name": "角色名称",
        "gender": "男",
        "age": 25,
        "appearance": "外貌描述",
        "background": "背景故事",
        "personality": "性格描述",
        "extra": {},
        "createTime": "2026-03-23T10:00:00"
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

### 2. 获取所有角色

**接口地址**: `GET /novelCharacter/all/{novelId}`

**路径参数**:
- `novelId`: 小说ID

**响应示例**:
```json
{
  "code": 200,
  "msg": "获取成功",
  "data": [
    {
      "id": 1,
      "novelId": 1,
      "name": "角色名称",
      "gender": "男",
      "age": 25,
      "appearance": "外貌描述",
      "background": "背景故事",
      "personality": "性格描述",
      "extra": {},
      "createTime": "2026-03-23T10:00:00"
    }
  ]
}
```

### 3. 获取角色详情

**接口地址**: `GET /novelCharacter/{id}`

**路径参数**:
- `id`: 角色ID

**响应示例**:
```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    "id": 1,
    "novelId": 1,
    "name": "角色名称",
    "gender": "男",
    "age": 25,
    "appearance": "外貌描述",
    "background": "背景故事",
    "personality": "性格描述",
    "extra": {},
    "createTime": "2026-03-23T10:00:00"
  }
}
```

### 4. 创建角色

**接口地址**: `POST /novelCharacter/create`

**请求参数**:
```json
{
  "novelId": 1,
  "name": "string",
  "gender": "string",
  "age": 25,
  "appearance": "string",
  "background": "string",
  "personality": "string",
  "extra": {}
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "创建成功",
  "data": {
    "id": 1,
    "novelId": 1,
    "name": "角色名称",
    "gender": "男",
    "age": 25,
    "appearance": "外貌描述",
    "background": "背景故事",
    "personality": "性格描述",
    "extra": {},
    "createTime": "2026-03-23T10:00:00"
  }
}
```

### 5. 更新角色

**接口地址**: `POST /novelCharacter/update/{id}`

**路径参数**:
- `id`: 角色ID

**请求参数**:
```json
{
  "name": "string",
  "gender": "string",
  "age": 25,
  "appearance": "string",
  "background": "string",
  "personality": "string",
  "extra": {}
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "更新成功",
  "data": {
    "id": 1,
    "novelId": 1,
    "name": "更新后的角色名称",
    "gender": "女",
    "age": 26,
    "appearance": "更新后的外貌描述",
    "background": "更新后的背景故事",
    "personality": "更新后的性格描述",
    "extra": {},
    "createTime": "2026-03-23T10:00:00"
  }
}
```

### 6. 删除角色

**接口地址**: `POST /novelCharacter/delete/{id}`

**路径参数**:
- `id`: 角色ID

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null
}
```

## RAG 聊天接口

### 1. GET 方式聊天

**接口地址**: `GET /chat?message=xxx&useRag=true`

**查询参数**:
- `message`: 用户消息
- `useRag`: 是否启用 RAG，默认为 true

**响应示例**:
```
AI 的回答内容
```

### 2. POST 方式聊天（推荐）

**接口地址**: `POST /chat`

**请求参数**:
```json
{
  "message": "string",
  "useRag": true,
  "characterIds": [1, 2],
  "storyNodeIds": [3, 4],
  "includeCharacters": true,
  "includeStory": true
}
```

**响应示例**:
```
AI 的回答内容
```

## 故事节点接口

### 1. 获取所有故事节点

**接口地址**: `GET /storyNode/all/{novelId}`

**路径参数**:
- `novelId`: 小说ID

**响应示例**:
```json
{
  "code": 200,
  "msg": "获取成功",
  "data": [
    {
      "id": 1,
      "novelId": 1,
      "timelineId": 1,
      "title": "节点标题",
      "content": "节点内容",
      "createTime": "2026-03-23T10:00:00",
      "vectorId": "vector_id_123"
    }
  ]
}
```

## 错误码说明

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

## 注意事项

1. 所有需要认证的接口都需要在请求头中携带 JWT Token：
   ```
   Authorization: Bearer <token>
   ```

2. 文件上传接口需要使用 multipart/form-data 格式

3. 时间格式统一使用 ISO 8601 标准：`yyyy-MM-dd'T'HH:mm:ss`

4. 分页参数从 1 开始计数

### 已修复问题

| 问题 | 文件 | 修复内容 |
|------|------|---------|
| cover_url 字段丢失 | `NovelMapper.xml` L11 | `n.cover_url AS cover` → `n.cover_url`（去掉错误别名，MyBatis 自动映射到 coverUrl） |
| 类名拼写错误 | `UserServiceIml.java` | 重命名为 `UserServiceImpl.java` |

---
*文档版本: v1.1*
*最后更新: 2026-05-31*