# RAG 处理依赖（在现有 requirements.txt 基础上额外需要）
# 安装：pip install -r requirements-rag.txt

# 核心 RAG 依赖
sentence-transformers>=3.0.0
chromadb>=1.5.9
langchain-text-splitters>=0.3.4
langchain-huggingface>=0.2.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# 进度条
tqdm>=4.66.0

# 配置管理
pyyaml>=6.0.0

# 可选：重排序模型
# torch>=2.0.0  # 已在 pyproject.toml.toml.toml 中指定 CPU 版本
# transformers>=4.35.0  # 重排序需要

# 开发/测试
pytest>=8.0.0
pytest-asyncio>=0.23.0